# Various Utilities to be Installed

`docker.md` - Docker

`openvpn.md` - OpenVPN

`remote-desktop.md` sets up remote desktop via RDP or VNC.

`watchdog.md` is useful to set up watchdog, to reboot Raspberry Pi when it crashes.